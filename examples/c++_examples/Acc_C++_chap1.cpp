//
// Created by rico on 5/15/19.
//

#include <iostream>
#include <string>
#include <iomanip>
using std::cout;
using std::cin;
using std::endl;
using std::string;
using std::setprecision;
int main()
{
	std::ve
    /*std::string name;
    std::cout<<"what is your name?"<<std::endl;
    std::cin>>name;//what's your name?
    const std::string msg = "Hola"+name+"!";
    std::string::size_type length = msg.size()+2;// get length
    std::cout<<std::endl;
    for (int i=0;i<5;++i)// count times5
    {
        for(std::string::size_type c=0;c<length;++c){
            if(i==0||i==4)// if meet certain conditions, then
                std::cout<<"*";
            else
            {
                if(i==1||i==3)
                {
                    if(c==0||c==length-1)
                        std::cout<<"*";
                    else
                        std::cout<<" ";
                }
                else
                {
                    std::cout<<"*"<<msg<<"*";
                    break;
                }
            }
        }
        std::cout<<std::endl;
    }*/
    double lol;
cout<<lol;
}
